# PDF編集（pdf.html）

PDFをブラウザで開いて、書き込み・ページ整理・文字の差し替えをして保存できるWebアプリです。
処理はすべて閲覧している端末の中で完結し、ファイルはどこにも送信されません。

---

## 公開URL

- ツール本体: https://akiranakachi-eng.github.io/tools/pdf.html
- リポジトリ: https://github.com/akiranakachi-eng/tools

構成は次のとおりです。`pdf-assets/` の中身（約5.6MB）はすべて必要です。
ライブラリと日本語フォントを同梱しているので、外部CDNには一切つながりません。

```
tools/
├── index.html          ← ツール一覧のトップページ
├── pdf.html            ← PDF編集
├── pdf-assets/         ← ライブラリ・日本語フォント・CMap
└── .nojekyll           ← Jekyll処理を飛ばす（消さないこと）
```

---

## できること

### 上に載せる
| ツール | 内容 |
|---|---|
| 文字 | クリックして日本語を入力。Enterで改行、ダブルクリックで再編集 |
| ペン / 直線 / 矢印 | 手書き・線・矢印（Shiftで水平垂直） |
| 四角 / 楕円 | 枠線・塗りつぶし・不透明度 |
| マーカー | 蛍光ペン（下の文字が透ける乗算合成） |
| 白塗り | 元の内容を隠す。背景色に合わせて色を変更可 |
| 画像 | 印影・サインなどのPNG/JPGを貼り付け |

### ページ操作
左のサムネイルから、**ドラッグで並べ替え**、⟳回転、⧉複製、✕削除。
「PDFを追加」で別のPDFを読み込むと、末尾に結合されます。

### 文字差し替え
「文字差替」ツールに切り替えると、元のPDFの文字が青い枠で表示されます。
枠をクリックすると、背景色でその文字を塗りつぶし、同じ位置・同じ大きさで
書き換えられる状態になります。

- 元のフォントまでは再現できないため、**見た目は少し変わります**
- 背景が写真や網掛けだと、塗りつぶしが目立つことがあります
- スキャンしただけのPDF（画像のみ）は文字データを持たないため、この機能は使えません
  → その場合は「白塗り」＋「文字」で同じことができます

### 保存
「保存」で `元のファイル名_編集.pdf` としてダウンロードされます。

- 標準では、書いた文字は**検索・コピーできるテキスト**として埋め込まれます（Noto Sans JP を同梱）
- 「文字を画像として焼き込む」にチェックを入れると、画面の見た目そのままを画像化して埋め込みます
- 「フォントを指定」から手持ちの .ttf / .otf を読み込むと、そのフォントで埋め込めます

#### ⚠ 白塗り・文字差し替えと「元の文字が残る」問題

白塗りや文字差し替えは、**元の文字の上に図形や新しい文字を重ねているだけ**です。
見た目は隠れますが、元の文字データはPDFの中に残っているため、
検索やテキストコピーをすると読めてしまいます。

金額や氏名の訂正など、元の内容が読めては困る場合は
**「元の文字も完全に消す（ページを画像化）」**にチェックを入れて保存してください。
全ページを画像に置き換えるので、隠した内容は復元できなくなります。

| | 通常保存 | 画像化して保存 |
|---|---|---|
| 元の文字が残るか | 残る（検索で読める） | 残らない |
| 文字の検索・コピー | できる | できない |
| ファイルサイズ | 小さい | 1ページ40〜100KB程度 |
| 画質 | 元のまま | 170dpi相当

---

## ショートカット

| キー | 動作 |
|---|---|
| V / T / E / P / R | 選択 / 文字 / 文字差替 / ペン / 四角 |
| Ctrl+Z ／ Ctrl+Shift+Z | 元に戻す ／ やり直す |
| Ctrl+S | 保存 |
| Delete | 選択中の注釈を削除 |
| 矢印キー | 1pt動かす（Shiftで10pt） |

何もない場所をドラッグすると、ページをスクロールできます（iPhoneでも同様）。

---

## 制限・注意点

- **パスワード付きPDF**は開けません。先に解除してからお使いください。
- **入力欄付き（フォーム）のPDF**は、保存時に入力内容が固定されます（記入済みの状態で紙に落ちるイメージ）。保存後は欄の再入力ができません。
- 元のPDFに埋め込まれた文字の**フォントそのものの差し替え**はできません。
- **白塗り・文字差し替えでは元の文字データが残ります。**上記のとおり、必要なら「画像化して保存」を使ってください。
- 動作確認はChrome（PC）とiOS Safari想定です。ローカルの `file://` で直接開くと
  フォントやCMapの読み込みが制限されるため、GitHub Pages などのURL経由で開いてください。

---

## 使用しているもの

- [pdf.js](https://github.com/mozilla/pdf.js)（表示・文字位置の解析） — Apache-2.0
- [pdf-lib](https://github.com/Hopding/pdf-lib)（PDFの書き出し） — MIT
- [@pdf-lib/fontkit](https://github.com/Hopding/fontkit)（フォント埋め込み） — MIT
- [Noto Sans CJK JP](https://github.com/notofonts/noto-cjk)（日本語フォント／サブセット） — OFL-1.1

詳細は `pdf-assets/LICENSES.txt` を参照してください。

# タスクボード（出先用）

Googleドライブにある `タスクボード.xlsx` を、iPhoneからアプリのように見て・答えるための画面です。

公開URL: https://akiranakachi-eng.github.io/tools/taskboard.html

初回は接続の設定画面が開きます。下の「つなぎ方」でURLと合言葉を入れるまで、項目は出ません。設定はこの端末に残るので、次からは入力不要です。

---

## 何ができるか

- ボードに残っている項目を一覧で見る
- カードをタップして詳細画面を開き、本文を端折らずに読む
- 添えられた「見るもの・開くもの」を開く(Googleドライブの中のものはタップでそのまま)
- 添えられた資料(md)を、**この画面の中で整形して読む**(見出し・箇条書き・表・コード)
- 検収チェックポイントを1つずつ、または「すべてチェック」でまとめて入り切りする
- 終わったタスクを「済にする」
- 出来が駄目だったものを、理由を付けて「差し戻す」(やり直してもらう)
- 確認依頼に答える（台帳の「選択肢」列が埋まっていればボタン、それ以外は自由記入）
- 完了ずみの履歴を見る
- ホーム画面に追加して、アプリのアイコンから開く

検収チェックは、押した瞬間に画面へ反映し、送信は裏で行います。返事を待たないので、続けて押しても取りこぼしません。送れなかったときだけ、チェックを元に戻して知らせます。

---

## 仕組み

台帳を壊さないように、iPhoneからは**台帳を直接書き換えません**。

```
              ① 見る（読むだけ）
  iPhone  ──────────────────────────►  タスクボード.xlsx
      │                                      ▲
      │  ② 押した内容を置く                   │ ③ PCが読んで台帳に反映
      └──────►  タスクボード_受信箱  ─────────┘
                （小さなファイルが1件ずつ貯まるフォルダ）
```

読むときは、xlsx を一時的にスプレッドシート形式へ複製して中身を読み、読み終わったら複製を消します。台帳そのものには触れません。

答えたり済にしたりすると、`タスクボード_受信箱` フォルダにファイルが1つ置かれるだけです。PCとiPhoneが同時に台帳を書いて片方が消える、という事故を防ぐためです。

このため、押してもすぐには消えません。PCが受け取って台帳に書くまで、カードには「送信済み — PCが受け取るのを待っています」と出ます。

---

## つなぎ方

### 1. Apps Script を作る

1. https://script.google.com を開いて「新しいプロジェクト」
2. 左の `コード.gs` の中身を全部消して、この中身を貼り付ける
   → [`taskboard-assets/gas/コード.gs`](taskboard-assets/gas/コード.gs)
3. プロジェクト名を「タスクボード連携」などに変える

### 2. 合言葉を決める

貼り付けたコードの上のほうにある `TOKEN` を、長いランダムな文字列に書き換えます。

```js
TOKEN: 'ここを長いランダムな文字列に書き換える',
```

例: `tb-9f3k2mQx7Zp1LvR8dY4nWsA6` のような、推測できない30文字くらいのもの。

これを知っている人だけが台帳の中身を見られます。**人に見せないでください。**

### 3. Drive API を使えるようにする

xlsx を読むために必要です。

1. 左側の「サービス」の右の **＋** を押す
2. 一覧から **Drive API** を選ぶ
3. バージョンが **v3** になっていることを確認して「追加」

### 4. 動くか確かめる

1. 上の関数名の欄で `動作確認` を選んで「実行」
2. 初回は許可を求められるので、自分のアカウントで承認する
   （「このアプリは確認されていません」と出たら、詳細 → 安全でないページに移動、で進めます。自分で書いたスクリプトなので問題ありません）
3. 実行ログに `ボード: ○件 / 完了ずみ: ○件` と出れば成功

### 5. ウェブアプリとして公開する

1. 右上の「デプロイ」→「新しいデプロイ」
2. 歯車 → 種類として **ウェブアプリ** を選ぶ
3. 「次のユーザーとして実行」→ **自分**
4. 「アクセスできるユーザー」→ **全員**
5. デプロイして、出てきた `https://script.google.com/macros/s/..../exec` というURLをコピー

> **なぜ「全員」なのか**
> iPhoneの画面（GitHub Pages）から呼ぶには、この設定が要ります。
> 代わりに合言葉で守っています。URLだけ知っていても合言葉が違えば中身は返しません。
> 逆に言うと、URLと合言葉の両方が漏れると台帳が読めてしまいます。両方を人に渡さないでください。

### 6. iPhone側で設定する

1. Safariで https://akiranakachi-eng.github.io/tools/taskboard.html を開く
2. 右上の **⚙** を押す
3. コピーしたURLと、決めた合言葉を入れて「保存してつなぐ」
4. 本物のタスクが出れば完了

設定はその端末の中だけに保存されます。

### 7. ホーム画面に追加する

Safariの共有ボタン（□に↑）→「ホーム画面に追加」。
アドレスバーの無いアプリのような表示になります。

---

## PC側にやってもらうこと

このままだと、iPhoneで押した内容が受信箱に貯まるだけで台帳に反映されません。
PC側のClaudeに、次の内容を伝えてください。

> Googleドライブの `タスクボード_受信箱` フォルダを、タスクボードを確認するタイミングで一緒に見てください。
> 中にある `.json` ファイルを、古いものから順に処理します。中身はこの形です。
>
> ```json
> { "受信日時": "2026-08-10 21:30:45", "ID": "T0012", "種別": "回答",
>   "回答": "「式」でまとめる", "補足": "急ぎでなければこれで" }
> ```
>
> ```json
> { "受信日時": "2026-08-11 09:12:03", "ID": "T0020", "種別": "チェック",
>   "チェック番号": 3, "状態": "済" }
> ```
>
> ```json
> { "受信日時": "2026-08-11 16:50:02", "ID": "T0031", "種別": "差し戻し",
>   "コメント": "合計が内訳と合っていません" }
> ```
>
> - 種別が `済` … `タスクボード.xlsx` のボードシートで同じIDの行を探し、「済」列に `済` と入れる
> - 種別が `回答` … 同じIDの行の「回答」列に回答を、「補足」列に補足を入れる
> - 種別が `チェック` … 台帳には書かず、`python cli.py check <ID> <チェック番号> --by 中地` を実行する(状態が `未` なら `uncheck`)。検収の記録は tasks.json が正で、xlsx はそこから書き直されるため、xlsx に手で書いても次の書き出しで消えます
> - 種別が `差し戻し` … 台帳には書かず、`python cli.py sashimodoshi <ID> --comment "<コメントの全文>" --by 中地` を実行する。未着手に戻り、理由が履歴と本文に残り、次アクションが設定されていればその理由を添えてClaudeが動き出します
>
> 台帳に書き終えたら、そのjsonファイルを削除してください。
> 該当するIDがボードに無いときは、既に片付いているので、そのままファイルを削除して構いません。
> `チェック` で番号が範囲外だったり観点が無かったりしたときも、そのファイルは削除して、あとで中地さんに一言伝えてください。残しておくと毎回同じ失敗を繰り返します。

削除するところまでやってもらうのが大事です。残っていると同じ回答を何度も処理してしまいます。

`チェック` は同じ番号を続けて送れます(入れ直し・外し直し)。受信日時の古いものから順に処理すれば、最後に押した状態に落ち着きます。

---

## 制限・注意

- **反映までの内訳(実測)**: 台帳→xlsx 約2秒 / **ドライブ同期 約15秒**(ここは縮められません) / GASの読み取り 約2秒 / 携帯の取り直し 30秒ごと。放っておくと**20〜50秒**、⟳を押せば**2〜4秒**で映ります。
- **⟳は取り直しの最中に押しても捨てません。** 走っているぶんが終わり次第、続けてもう一度取りに行きます。「最終取得」は端末側で受け取った時刻を**秒まで**出すので、押せば必ず数字が動きます。
- **PCの電源が入っていないあいだは処理されません。** 次にPCが動いたときにまとめて実行されます。
- **選択肢のボタンは台帳の「選択肢」列から作ります。**(下の「選択肢ボタンの作られ方」を参照)
- **圏外のとき**は最後に読めた内容を表示します。画面下に「つながらないため前回の内容を表示中」と出ます。
- **IDは書き換えないでください。** PC側が行を見分ける目印です。
- 台帳の列（済 / 回答 / 補足 / ID / 種別 / 状態 / 担当 / 内容 / 期限 / 本文・説明 / 添付 / 選択肢）を変えると、この画面も直す必要があります。「資料」は写し(JSON)だけに入る項目で、xlsxの列ではありません。
- **添付(見るもの・開くもの)は台帳の「添付」列から読んでいます。** 既存の並びは動かさず、右に足した列です。1行に1件、`ラベル|場所` の形。場所はPCから見た道(`G:\マイドライブ\…` や `G:\共有ドライブ\…`)なので、そのままでは携帯で開けません。GAS側が名前をたどってファイルIDを見つけ、開けるURLに読み替えて返します(`links` の欄に `{場所: URL}`)。
  - **読み替えは1件ずつ独立です。** 1つたどれなくても、ほかの添付はそのまま返します。ボード自体も、読み替えが丸ごと失敗したときは空の `links` を付けて必ず返します。
  - 見つかったものはスクリプトのプロパティに**ずっと覚えます**(2回目からはAPIを1回も呼ばずに返る)。見つからなかったものは60秒だけ覚えて、そのあとまたたどり直します。
  - 1回の読み取りで**新しくたどるのは5件まで**です。たどるのは1件につき階層のぶんだけAPIを呼ぶので、添付の多いボードで一度に全部やると返事が遅くなります。溢れたぶんは次の読み取り(1分ごとの自動更新)で埋まります。
  - ファイルを動かして古いURLが残ってしまったときは、exec URL に `&relink=1` を付けて一度読むと、覚えた読み替えを捨ててたどり直します(合言葉が要ります)。
  - **PCの中のファイルや bat は読み替えられません。** 携帯では「PCで開くもの」と薄く出るだけです。PCのタスクボードから開いてください。
  - **mdは「資料」として別に届きます。** 添付の道をたどって開くのではなく、PC側が `タスクボード_資料` フォルダへコピーしたものを画面の中で読みます(上の「資料(md)を携帯で読む仕組み」を参照)。
  - 付け方: `python cli.py add --title "…" --links "図面|G:\共有ドライブ\…\A棟.pdf" --links "起動|C:\tools\run.bat"`(1件につき `--links` を1回。ラベルは省略できます。`edit --links ""` で全消し)
  - PCの画面では本文の下にボタンが並び、押すと既定のアプリで開きます(画像はビューア・フォルダはエクスプローラ)。`.bat` `.exe` などの実行するものは、押し間違い防止に一度確認します。
- **検収チェックポイントは「本文・説明」から読んでいます。** PC側が本文の末尾に `【検収 1/3】` と `☑ / ☐ 観点` を書き出しているので、そこを切り出して詳細画面の一覧にしています。台帳に列は足していません。行数が見出しの数と合わないときは、取り違えを避けるため一覧を出さず、本文のまま表示します。
- **設定はこの端末の中(localStorage)に残ります。** 保存名は `taskboard_config_v1` で、保存したあとに読み直して残ったかを確かめています。残せなかったときは設定画面に赤字で理由を出すので、黙って入力し直しになることはありません。
- **ホーム画面に追加したアプリとSafariとで、設定が別々になることがあります。** 両方から使うなら、それぞれで一度ずつ設定してください。プライベートブラウズでは端末に残せません。
- **未設定のときは項目を出しません。** 見本データを出すと、押しても送られないのに送れたように見えるためです(実際にこれで検収チェックが届かないことがありました)。
- **台帳の写し(`タスクボード.json`)を xlsx と一緒に置いています。** GASは xlsx をそのままでは読めず、毎回スプレッドシートに複製してから読んでいました(1回6秒)。同じ中身の素のJSONを並べておけば1回読むだけで済みます(**実測 2.0秒**)。xlsxは今までどおり残るので、スプレッドシートで開く使い方は変わりません。写しが無い・壊れているときは、これまでどおり xlsx を複製して読みます。返事の `source` が `json` か `xlsx` かで、どちらの道で読んだか分かります。
- **開いているあいだは30秒ごとに台帳を取り直します。** 画面下の「最終取得」の時刻が進みます。背面に回っているあいだは止まり、戻ってきたときにその場で1回取り直します。文字を入力している最中は見送ります(取り直すと画面を作り直すため、書きかけが消えないように)。自動のぶんは`fresh`を付けず、向こうの15秒の取り置きに任せます(こちらから送ったときは向こうが取り置きを捨てるので、送った直後は取り置き無しでも最新が返ります)。
- **更新は2種類あります。** 右上の⟳は台帳をいますぐ取り直すだけ(`fresh=1`で必ず最新)。画面の作りそのものを新しくするには、⚙の「アプリを再読み込み」を押してください。ホーム画面から開くとアドレス欄が無く、ふつうの再読み込みができないためです。設定画面には、いま動いている画面がいつのものかも出ます。
- **差し戻しは詳細画面のいちばん下から。理由は必須です。** 押すとカードに「差し戻し送信済み」と出て、そのまま残ります(済とは違って消しません。PCが受け取ると未着手に戻り、内容が変わって出直してきます)。理由は台帳の「本文・説明」のいちばん上に `【差し戻し 中地 08-11 16:51】` の形で載るので、カードの数行しか見えない場面でも目に入ります。
- **「済にする」を押した項目と、答えた確認依頼は、その場で一覧から消えます。** PCの反映を待ちません。消したことはこの端末だけに覚えておき、台帳が片付いたら忘れます。**一日たっても片付かなければ一覧に出し直します**(PCの電源が入っていない・受信箱が処理されていない場合に、済にしたつもりの用事が誰にも見えなくなるのを防ぐため)。
  - 覚えておくのは**送信が成功したものだけ**です。送れなかったときは何も覚えないので、カードはそのまま残ります。
  - 覚え書きが際限なく溜まることはありません。台帳を取り直すたびに、**いま一覧に出るはずのもの以外は捨てます**(片付いたもの・一日たったもの)。送信が成功しないと1件も増えないので、圏外で何度押しても溜まりません。
  - **差し戻しだけは消しません。**やり直したものが出直してくるまでのあいだ、頼んだこと自体が見えなくなってしまうためです。カードには「差し戻し送信済み」と出ます。
- **PC側でプログラムを直したら、ウォッチャーは自分で動かし直します。** 常駐したまま `.py` を差し替えても、動いているのは読み込み済みの古い中身です。中身が変わったことに気づいたら、新しい版で立ち上げ直します(書きかけで読めないうちは、古い版のまま動き続けます)。起動のたびにxlsxを1回書き直すので、直した内容がすぐ出先に出ます。

---

## 資料(md)を携帯で読む仕組み

添付が `.md` のとき、これまでは携帯から中身を見る手立てがありませんでした(ドライブの道は携帯から開けず、開けてもmdは素のテキストで出るだけ)。カードに**「📖 …(この画面で読む)」**が出るようになり、タップするとアプリの中で整形して読めます。

```
  PC側(sheet.py)                    Googleドライブ                iPhone
  添付のmdをコピー ──────────►  タスクボード_資料\○○.md
  写しに 資料 を書く ──────────►  タスクボード.json
                                          │
                                          │  ① 一覧といっしょに「資料」を受け取る
                                          ▼
                                    GAS(doGet) ──────────────►  カードに📖ボタン
                                          ▲
                                          │  ② タップしたときだけ ?md=<ファイル名>
                                          └───────────────────  中身を1件だけ取りに行く
```

### 台帳側の形

PC側が写し `タスクボード.json` の各行に「資料」を書きます。

```json
"資料": [{ "label": "見積の考え方", "file": "T0031_見積の考え方.md" }]
```

`file` は `タスクボード_資料` フォルダにコピーされたファイルの**名前だけ**です。`label` は画面に出す見出しで、省略するとファイル名が出ます。「資料」が無い台帳・空の行では📖ボタンが出ないだけで、ほかは今までどおり動きます。

### 読めるのは資料フォルダの中だけ

携帯が送るのはファイル名だけで、道は送りません。GAS側も `タスクボード_資料` フォルダの**直下を名前で1件引くだけ**なので、台帳やほかのフォルダは読めません。念のため、区切り文字(`/` `\`)や `..` を含む名前はその場で断ります。512KBを超えるものも断ります(携帯で読み切れないため)。

資料フォルダは**GASが作りません**。無いときは「置き場が見つかりません」と返します。空のフォルダを作ってしまうと、PC側がまだ置いていないのか、置いたのに届いていないのかが分からなくなるためです。

### 表示について

外部のライブラリは使えない(この画面は1枚のHTMLで完結させている)ので、mdを読める形にするところは自前です。見出し・箇条書き(入れ子)・番号付き・チェックボックス・表(罫線と桁揃え)・引用・コード・区切り線・太字/斜体/打ち消し/リンクに対応します。**mdに書かれた生のHTMLは出しません**(文字として出ます)。リンクは `http` `https` `mailto` だけを開けるようにしています。

表は折り返さず横にスクロールします。畳むと数字の並びが読めなくなるためです。

一度開いた資料はその場に覚えておくので、二度目からは待たずに出ます(アプリを開き直すと忘れます)。まだドライブの同期が終わっていないときは「まだ届いていません」と出るので、少し待ってもう一度押してください。圏外のときは「つながりませんでした」と出て、「もう一度読む」ボタンが出ます。

### GASの再デプロイが要ります

この機能は `コード.gs` に手を入れています。**貼り替えたうえで、デプロイし直さないと携帯からは使えません。**

1. https://script.google.com でプロジェクトを開き、`コード.gs` を [`taskboard-assets/gas/コード.gs`](taskboard-assets/gas/コード.gs) の中身で置き換える(`TOKEN` は自分のものに戻すのを忘れずに)
2. 「デプロイ」→「デプロイを管理」→ 鉛筆(編集)→ バージョンを **「新バージョン」** にして「デプロイ」

**URLは変わりません。**iPhone側の設定を入れ直す必要はありません。「新しいデプロイ」を選ぶと別のURLになってしまうので、必ず既存のデプロイを編集してください。

うまくいったかは、エディタで `動作確認` を実行すると分かります。資料フォルダが見つかれば、その場所と中のファイル名が実行ログに出ます。

---

## 選択肢ボタンの作られ方

確認依頼のカードと詳細画面に出る回答ボタンは、**台帳の「選択肢」列を第一の情報源とします。**

PC側(`sheet.py`)が、写し `タスクボード.json` と `タスクボード.xlsx` の両方に「選択肢」を書き出します。既存の並びは動かさず、「添付」のさらに右に足した列です。値の形はどちらも同じで、パイプ区切りの1本の文字列です。

```
「式」でまとめる|「㎡」のまま出す|どちらでもよい
```

選択肢の無いカードは空文字です。

| 台帳の「選択肢」 | 画面の出方 |
| --- | --- |
| 値がある | パイプで割ってボタンにする(最大6件・頭に `1.` `2.` と番号を振る) |
| 空文字 | 本文からの推測にまわる |
| 列そのものが無い | 本文からの推測にまわる |

**本文からの推測はフォールバックです。** 本文に `A: …` `B: …` や `1. …` `2. …` のような行が2つ以上あるとボタンになります。PC側の列追加がまだ行き渡っていない台帳でも今までどおり動くように残してあります。推測も効かないときは自由記入になります。

どの道を通っても、押したときに「回答」列へ入る文字列は選択肢の文言そのままです。頭の `1.` `A.` は画面上の見出しで、送る値には含みません。回答の送り方(受信箱へ1件置く)も変わりません。

Apps Script(`コード.gs`)は台帳の見出し行をそのまま `{見出し: 値}` に変えて返す作りなので、「選択肢」列の追加にあたって**GAS側の変更も再デプロイも要りません**。

---

## 見た目について

配色は既存のタスクボード（`sumitsubo.py` のダークテーマ）から持ってきています。
色を変えたいときは `taskboard.html` の先頭にある `:root{...}` の部分だけを触れば全体に反映されます。
